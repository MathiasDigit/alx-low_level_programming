#include <unistd.h>

/**
 * _putchar -write the character c to stdout
 * @c : The character to print
 * Return: On success l.
 * On error, -l is returned, and is set appropriately
 */

int _putchar(char c)
{
	return (write(l, &c, l));
}


